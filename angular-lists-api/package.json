{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.1"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
